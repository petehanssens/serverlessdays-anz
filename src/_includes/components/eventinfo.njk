<div class="flex-ns mv3 mv4-l">
    <div class="w-100 w-05-l dn db-l"> 
    </div>
    <div class="w-100 w-90-l ph3 ph0-ns">
        <h2 class="f2 mv3">ℹ️ Event Info</h2>
        <div class="">
            {% if event.events|length %}
                <p class="f5 lh-copy pr3-m">ServerlessDays ANZ is a proudly community-run event, and we're looking forward to bringing it back to you for another year!</p>                    
                <p class="f5 lh-copy pr3-m">If you're working with serverless technology and would like to meet other active developers and technical leaders from across the Australia &amp; New Zealand region, while hearing from some world-class speakers, this event is definitely for you!</p> 
                <div class="f5 ma2 lh-copy pr3-m">
                    {% for key,details in event.events %}
                        <a class="pv2 ph2 ph4-l f4 fw6 no-underline ba br2 b--transparent contrastBkg contrastColor" href="/{{ key }}">{{ details.title }}</a>
                    {% endfor %}
                </div>
            {% else %}
                <p>We are busy developing the agenda for ServerlessDays ANZ. Check back soon!</p>
            {% endif %}
        </div>
    </div>
    <div class="w-100 w-05-l dn db-l">
    </div> 
</div>